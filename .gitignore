# lol, what are you searching for? :)

__pycache__/
*.pyc
*.log
*.bak
# Env files — match every variant, not just bare ".env". `.env.example` is
# re-included below because it's the public template.
.env
.env.*
.env-*
!.env.example
.DS_Store
.claude/
.pytest_cache/
qr.png
old/
obsolete/
debuglog.txt
groups-*.json
memo.txt
CLAUDE.md
groups.json

# Auth secret auto-generated by app_core/auth_bootstrap.py at first run.
.auth-secret

# signal-cli account databases — never commit (contain session state / IDs).
*.db
account.db

# Signal envelope / group-sync dumps — never commit raw intel payloads
envelope-*.json
envelopes-*.json
envelope_dump*
groupsync-*.json
raw_envelope*
*.envelope.json
